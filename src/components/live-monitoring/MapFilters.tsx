import { Search, SlidersHorizontal } from 'lucide-react'
import type { LiveStatus } from './types'

const statuses: Array<{ value: 'all' | LiveStatus; label: string }> = [{ value: 'all', label: 'All sensors' }, { value: 'normal', label: 'Normal' }, { value: 'warning', label: 'Warning' }, { value: 'critical', label: 'Critical' }, { value: 'offline', label: 'Offline' }]
export function MapFilters({ sites, selectedSite, onSiteChange, status, onStatusChange, search, onSearchChange }: { sites: Array<{ id: string; name: string }>; selectedSite: string; onSiteChange: (id: string) => void; status: 'all' | LiveStatus; onStatusChange: (status: 'all' | LiveStatus) => void; search: string; onSearchChange: (search: string) => void }) {
  return <div className="map-filters"><div className="filter-heading"><SlidersHorizontal size={14} /><span>Filter sensors</span></div><label className="filter-select"><span>Site</span><select value={selectedSite} onChange={(event) => onSiteChange(event.target.value)}><option value="all">All demo sites</option>{sites.map((site) => <option key={site.id} value={site.id}>{site.name}</option>)}</select></label><div className="status-filter" role="group" aria-label="Sensor status"><span>Status</span><div className="status-filter-buttons">{statuses.map((item) => <button key={item.value} className={status === item.value ? 'selected' : ''} onClick={() => onStatusChange(item.value)}>{item.label}</button>)}</div></div><label className="search-filter"><Search size={14} /><input value={search} onChange={(event) => onSearchChange(event.target.value)} placeholder="Search sensors" aria-label="Search sensors" /></label></div>
}
